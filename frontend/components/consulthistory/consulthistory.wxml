<view class="box">
  <view class="content">
    <view>【时间】{{time}}</view> 
    <view>【问题】{{question}}</view>
    <view wx:if="{{isAsk}}">【回答】{{answer.length>0 ? "点击查看全部回答" : "暂未收到回答"}}</view>
    <view wx:if="{{!isAsk}}" class="notice">点击卡片参与回答</view>
  </view>
</view>